# Pines del agente de WhatsApp.
#
# Fuente única: PINES.md. Un número que esté acá y no allá está mal acá.
# Se fija con ==, nunca con un piso: un piso instala la versión que salió ayer,
# que no probó nadie. Verificado contra PyPI el 2026-08-17.
#
# Python 3.11 a 3.14. El contenedor corre 3.12; ver Dockerfile.

# ── runtime ──────────────────────────────────────────────────────────────────
fastapi==0.141.1
starlette==1.3.1          # fastapi no le pone techo: sin esta línea flota tres minors adelante
uvicorn==0.52.3

# los extras de uvicorn[standard], fijados uno por uno para que no se muevan por abajo
uvloop==0.22.1
httptools==0.8.0
watchfiles==1.2.0
websockets==17.0.1
anyio==4.14.2

anthropic==0.121.0
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.2
PyYAML==6.0.3

SQLAlchemy==2.0.52
asyncpg==0.31.0           # no aparece en ningún import: vive dentro de "postgresql+asyncpg://"
aiosqlite==0.22.1
psycopg2-binary==2.9.12   # driver síncrono del jobstore de APScheduler con Postgres
greenlet==3.5.5           # SQLAlchemy lo declara por marcador de plataforma: en una que no
                          #   esté listada no se instala, y el async muere en runtime
alembic==1.19.1

APScheduler==3.11.3       # el recordatorio de 24 h corre en el proceso, no en el cron de Railway
tenacity==9.1.4
structlog==26.1.0

# La rama `service_account` del paso 4: el token de Google pide un JWT firmado con RS256, y eso
# no se firma con la biblioteca estándar. No es google-auth: el chequeo 06 mapea la raíz `google`
# a google-api-python-client y sale rojo, y ese paquete trae su propio cliente HTTP.
PyJWT==2.13.0
cryptography==50.0.0      # el extra [crypto] de PyJWT, fijado aparte: `PyJWT[crypto]` pide
                          #   cryptography>=3.4.0, que es un piso y flota

# ── dev y pruebas ────────────────────────────────────────────────────────────
# La compuerta corre las pruebas adentro de la imagen, así que estas también se instalan.
pytest==9.1.1
pytest-asyncio==1.4.0
jsonschema==4.26.0
respx==0.23.1
freezegun==1.5.5
ruff==0.16.3
